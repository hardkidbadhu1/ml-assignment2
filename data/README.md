Raw dataset goes here (gitignored — only `test_data.csv` at the repo root is committed,
per the assignment brief and Streamlit Cloud's storage limits).

Download link and preprocessing notes are in the root `README.md`.
